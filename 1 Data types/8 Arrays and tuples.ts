//arrays-->holds multiple values of the same type

let numbers: number[] = [1, 2, 3, 4, 5];
let strings: string[] = ["apple", "banana", "cherry"];

//arrays generic syntax
let mixedArray: Array<string> = ["apple", "banana"];

//tuples-->holds multiple values of different types in a fixed order

//tuple-->fixed length array with specific types for each element
let tuple: [number, string, boolean] = [1, "apple", true];
