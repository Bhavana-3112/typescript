//function type-->iit can be used to define the type of a function, including its parameters and return type

function add(a: number, b: number): number {
  return a + b;
}

//arrow Function type

const greet = (name: string): string => `Hello, ${name}`;

